def printnums(count,increment):
	i = 0
	numbers = []

	while i < count:
		print "At the top i is %d" % i
		numbers.append(i)
		
		i += increment
		print "Numbers now: ", numbers
		print "At the bottom i is %d" % i
		
	print "The numbers: "

	for num in numbers:
		print num

for n in range(0,count,increment)
	numbers.append(n)
	print "Numbers now: ", numbers
	print "At the bottom, n is now %d" % n

